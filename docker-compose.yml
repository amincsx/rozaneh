version: '3.8'

# MongoDB services removed from this compose file.
# If you previously used MongoDB via docker-compose, its configuration
# has been deleted. Use Prisma/Postgres for database services instead.
